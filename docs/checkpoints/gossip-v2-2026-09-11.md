# Gossip v2 checkpoint — 2026-09-11

The active objective is to complete [issue #3](https://github.com/xpelch/gossip/issues/3),
with public evidence submission tracked in
[issue #29](https://github.com/xpelch/gossip/issues/29).

## Published state

Public Gossip branch: `codex/gossip-v2-conformance-final`

- `4c7892f` freezes `gossip.public-submission.v1` and the signed
  `gossip.public-submission-receipt.v1` contract.
- TypeScript and independent Python verification agree on canonical requests,
  receipts, digests, signatures, public lineage, and zero economics.
- Last complete public run: 179 passed, 2 platform-specific skips, with build
  and typecheck passing.

Sherwood branch: `codex/gossip-v2-conformance-final`

- `f0be4579` adds the public-only transactional submission store and migration
  0141.
- `c4e22adc` adds the C# signed receipt contract and exact public evidence
  reader.
- `9dd25a13` persists signed public receipts atomically in migration 0142.
- New submissions cannot commit without a signed receipt. Retries and restarts
  verify the historical signer and return the original receipt bytes. Receipt
  signing failure rolls back the operation, evidence, links, and receipt.
- Last full Gossip v2 run before the receipt persistence slice: 292/292.
- Final receipt persistence checks: 25/25 migration, store, and reader tests.

Pull requests:

- [Gossip #28](https://github.com/xpelch/gossip/pull/28)
- [Sherwood #464](https://github.com/xpelch/sherwood/pull/464)

## Deliberate boundaries

- Gossip exchanges, verifies, enriches, and propagates market intelligence.
- Gossip does not prepare, sign, submit, or execute trades or transactions.
- `private_submission`, `gossip_feedback`, payment, generic signing, and trading
  remain inaccessible.
- Frozen `gossip.evidence.v1` requires at least one source, and every source in
  a public submission must be public.

## Unfinished work

The public exact-digest retrieval contract is implemented on this branch:

- `src/public-evidence-document-v1.ts`
- `scripts/verify-v2-public-evidence-document.py`
- `test/fixtures/v2-public-evidence-document.json`
- `test/public-evidence-document-v1.test.ts`

Resume in this order:

1. Add one Sherwood facade and wire DTO shared by HTTP and MCP.
2. Register the public store and reader in dependency injection.
3. Activate root-authenticated `POST /v2/gossip/submissions` and exact-digest
   `GET /v2/gossip/evidence/{digest}`.
4. Activate `gossip_submit_v2` for root and explicitly scoped sessions, plus a
   root-only exact evidence lookup tool. Keep `gossip_feedback` blocked.
5. Advertise `public_submission` only after the transport process tests pass.
6. Run the full suites and two independent clean conformance runs against the
   final suite revision.

The active goal remains open. Do not mark Gossip v2 complete until both
transports, scoped sessions, capability truth, and final conformance evidence
are published.
